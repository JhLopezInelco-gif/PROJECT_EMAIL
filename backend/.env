# Database Configuration
# Usando SQLite para desarrollo (no requiere PostgreSQL)
# Para PostgreSQL, usa: postgresql://postgres:postgres@localhost:5432/email_sender
DATABASE_URL=sqlite:///./email_sender.db

# SMTP Configuration (FALLBACK - the app loads from database first)
# These values are only used if no active config exists in the DB
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=avisos@inelco.cloud
SMTP_PASSWORD=Cas2026*@
SMTP_FROM=avisos@inelco.cloud
SMTP_FROM_NAME=Avisos Inelco
SMTP_SECURITY=SSL
SMTP_TIMEOUT=30

# Security
SECRET_KEY=tu_clave_secreta_muy_segura_cambiar_en_produccion_12345678
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=24

# Admin User
ADMIN_USER=Admin1
ADMIN_PASSWORD=Admin123

# App Settings
APP_NAME=Sistema de Envio de Correos
DEBUG=True

# Email Settings
EMAIL_DELAY_SECONDS=2.0
EMAIL_BATCH_SIZE=50